git client for windows

git --version
git --help
git config --global user.name "fabiancornejom"
git config --global user.email "fabian.cornejo.mella1@gmail.com"

git config --list
clear
pwd
/c/Users/anandr72
mkdir Git_Demo
rm -Rf Git_Demo
cd Git_Demo/
clear
pwd
/c/Users/anandr72/Git_Demo
mkdir FirstRepo
cd FirstRepo/
pwd
/c/Users/anandr72/Git_Demo/FirstRepo
git init
clear
touch master.txt
notepad master.txt
This is my file!!
git status
untracked files master.txt

20 minutos

pwd

git init
Reinitialized existing Git repository

clear
git add .
git status
no commits yet
clear
git commit -m "This is my First Git Commit"
1 file changed
git status
nothing to commit working tree clean
git log
commit 131bdad2cbcddb (Head -> master)

clear
touch master1.txt
notepad master1.txt
This is the second file!!
notepad master.txt
Making some changes to it!!
git status
changes not staged to commit
clear
git add .
git status
changes to be commited
git commit -m "This is my Second Commit"
2 file changed, 4 insertions
clear
git log


github
new repository
FirstRepo
This is my First Github Repository on the Server
public
git remote add origin https://github.com/simplilearn-github/FirstRepo.git (url repositorio)
git remote -v
fetch
push

(si no funciona hay que quitar las credendiacles de windows)
git push -u origin master (aca se creo la rama master)
(la rama principal es la main)

login
username or email 
Password
enumetating objects 7, done.
refresh page github
master.txt
master1.txt


click en la descriipcion
ver el cambio

git bash
ssh-keygen -t rsa -C sandeep.d@simplilearn.net  (el correo)
Generating public/private rsa key pair
enter file (enter)
enter passphrase : empty (enter)
passphrase : enter again
the key randomart image is
create particular folder ssh key
/c/Users/
id_rsa
id_rsa.pub copy the las clave


github
settings
SSH and GPG keys
New SSH key
key
pegar la clave
Add SSH Key
password
confirm

clear
ssh -T git@github.com
yes
enter phrase : empty
you've successfully authenticated
new repository
SecondRepo
description : This is my Second Repository  that is created straigth on the Github Server (se agrega al readme)
public 
initialize repository with a readme
create repository


clone with ssh
copy url
cd..
pwd
/c/users/anadr72/git_demo
ls
FirstRepo/
clear
git clone (ssh url)
enter phrase (empty)
again(empty)
receiving objects: 100% 
ls -ltr
FirstRepo/
SecondRepo/


cd SecondRepo/
clear
ls -ltr
sep 16 readme.md
notepad second.txt
This is my second file that i will push on to the server
git status
nothin added to commit
clear
git add .
git commit -m "This is my First Commit for the second repo"
git remote -v
fetch 
push

git checkout -b master (si falla hay que crear la rama master)
git push origin master

enter phrase (empty)
compresing objects 100%
github
secondrepo
2 commits
this is my First Commit for the second repo
initial commit
9c965b5

40

git log --pretty=oneline
git reset --soft (codigo c3)
git log --pretty=oneline
(codigo c3)head->master
git stated
changes commited
notepad m6.txt
one new file that was missed out
git status
git add .
git commit -m "c4: re-writing history"
8 insertions 
2 deletions
git log --pretty=oneline

45

















